<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.demo.entity.User">
        <result column="userid" jdbcType="INTEGER" property="userid"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="userpassword" jdbcType="INTEGER" property="userpassword"/>
        <result column="status" jdbcType="VARCHAR" property="status"/>
        <result column="reserve1" jdbcType="VARCHAR" property="reserve1"/>
        <result column="reserve2" jdbcType="VARCHAR" property="reserve2"/>
        <result column="reserve3" jdbcType="VARCHAR" property="reserve3"/>
        <result column="reserve4" jdbcType="VARCHAR" property="reserve4"/>
    </resultMap>

    <select id="selectUserByUserId" resultType="com.example.demo.entity.User" parameterType="integer">
        select *
        from useraccount
        where userid = #{id}
    </select>
    <select id="selectUserByUserName" resultType="integer">
        select count(*)
        from useraccount
        where username = #{name}
    </select>
    <insert id="inserIntoUser" parameterType="com.example.demo.entity.User">
        insert into useraccount (userid, username, userpassword, status, reserve1, reserve2, reserve3, reserve4)
        values (#{userid}, #{username}, #{userpassword}, #{status}, #{reserve1}, #{reserve2}, #{reserve3}, #{reserve4})
    </insert>
    <select id="selectUserByNamePassWord" parameterType="string" resultType="list">
        select *
        from useraccount
        where username = #{username}
          and userpassword = #{password}
    </select>
</mapper>